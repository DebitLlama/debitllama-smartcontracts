import { expect } from "chai";
import { ethers } from "hardhat";

describe("It should deploy a contract and test it", function () {
  it("Should deposit, create payment intent and withdraw", async function () {
  });
});
